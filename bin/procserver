#!/usr/bin/env node

var path = require('path');
var fs   = require('fs');
var lib  = path.join(path.dirname(fs.realpathSync(__filename)), '../lib');

var Api = require(lib + '/api').Api;
var port = 8000;

api = new Api();
api.app.listen(port);
console.log("Started procserver on port", port);
